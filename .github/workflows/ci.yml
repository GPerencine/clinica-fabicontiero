name: Testes Automatizados (CI)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Baixando o código do repositório
      uses: actions/checkout@v4

    - name: Configurando o Node.js 18
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: 'backend/package-lock.json'

    - name: Instalando dependências (Backend)
      run: |
        cd backend
        npm ci

    - name: Verificação de Lint (Backend)
      run: |
        cd backend
        npm run lint

    - name: Rodando os Testes com Cobertura (Backend)
      run: |
        cd backend
        npm run test:coverage

  frontend:
    runs-on: ubuntu-latest
    steps:
    - name: Baixando o código do repositório
      uses: actions/checkout@v4

    - name: Configurando o Node.js 18
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: 'frontend/package-lock.json'

    - name: Instalando dependências (Frontend)
      run: |
        cd frontend
        npm ci

    - name: Realizando Build (Frontend)
      run: |
        cd frontend
        npm run build
